
export function gerarPrompt({ estilo, cores, fundo }) {
  return `Crie uma estampa com estilo ${estilo}, utilizando as cores ${cores} sobre um fundo ${fundo}. A estampa deve ser rica em detalhes, em alta resolução, tileável e com qualidade para impressão digital em tecido.`;
}
